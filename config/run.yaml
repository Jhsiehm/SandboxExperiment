run_id: phase1-e2012-smoke
epoch: e2012
models: [gpt-oss-2012ish, frontier-a, frontier-b]
question_set: data/questions/e2012.jsonl
max_tool_calls: 12
min_prominence: 0.0
n_questions: 50
category_balance:
  economic: 20
  legislative: 20
  geopolitical: 10
# Search sidecar: psbx sandbox up (libfaketime + --network none). Agent stays on host.
sandbox_mode: host
embedding_backend: hashing
# Offline reconstructed fixtures are allowed. Use `research` only with an
# authenticated frozen capture/artifact corpus; that mode fails closed otherwise.
evidence_use: practice
